# =============================================================================
# Memorial Site - 環境変数設定サンプル
# =============================================================================
# このファイルをコピーして .env.local を作成し、実際のURLを設定してください
# .env.local はGitに追跡されません

# Google スプレッドシートの公開CSV URL
# 取得方法: Google スプレッドシート → ファイル → 共有 → ウェブに公開 → CSV形式
CSV_URL=https://docs.google.com/spreadsheets/d/e/YOUR_SPREADSHEET_ID/pub?output=csv
